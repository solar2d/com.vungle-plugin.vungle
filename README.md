Sources for the plugin `plugin.vungle`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.vungle" = {
            publisherId = "com.vungle",
        },
    },
}
```
